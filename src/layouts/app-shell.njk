{% extends "layouts/base.njk" %}

{% block title %}{{ title or "CTI Agent Dashboard" }}{% endblock %}

{% block head %}
  <!-- ===================== Vendor Styles ===================== -->
  <link rel="stylesheet" href="./vendor/fontawesome/css/all.min.css">
  <script src="./vendor/fontawesome/js/all.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

  <!-- ===================== Page Styles ===================== -->
  <link rel="stylesheet" href="css/{{ page_css }}">
{% endblock %}

{% block body %}
  <!-- ===================== App Shell ===================== -->
  <div id="app-container" class="grid-layout" data-role="app-shell">
    <!-- ===================== Global Navigation ===================== -->
    {% include "partials/components/sidebar.njk" %}

    <!-- ===================== Page Content ===================== -->
    <main id="page-main" class="layout-main-content" data-role="page-main">
      {% block app_content %}{% endblock %}
    </main>
  </div>

  <!-- ===================== Server Data Payload ===================== -->
  <script id="page-data-json" type="application/json">{{ page_data | tojson }}</script>

  <!-- ===================== Client Data Bootstrap ===================== -->
  <script>
    (() => {
      const pageDataNode = document.getElementById('page-data-json');
      if (!pageDataNode) {
        window.__PAGE_DATA__ = {};
        return;
      }
      try {
        window.__PAGE_DATA__ = JSON.parse(pageDataNode.textContent || '{}');
      } catch (error) {
        window.__PAGE_DATA__ = {};
      }
    })();
  </script>
{% endblock %}
